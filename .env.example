# OpenClaw API (local)
VITE_OPENCLAW_API_URL=http://localhost:18789
VITE_OPENCLAW_TOKEN=your_gateway_token_here

# Firebase config is in src/lib/firebase.ts
# No need for env vars since it's public client config
